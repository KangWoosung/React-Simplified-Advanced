/*  2024-02-10 15:05:15

This one I couldn't catch up.


*/

// import React, { ForwardedRef, useRef } from "react";
// import Inner14, { Inner14Handle } from "./components/Input14";

const Component14 = () => {
  //   const inputRef = useRef<ForwardedRef<Inner14Handle>>(null);

  return (
    <>
      <h1>Component14</h1>
      {/* <button onClick={() => inputRef.current?.focus()}>Focus</button>
      {inputRef && <Inner14 ref={inputRef as React.RefObject<ReactElement>} />} */}
    </>
  );
};

export default Component14;
